# Database Setup Notes

Tables are created implicitly by the pipeline notebooks
(`saveAsTable`). `schema.sql` is a
reference for the resulting table shapes.

- Catalog: `workspace`, schema: `default` (Unity Catalog, Databricks
  Free Edition)